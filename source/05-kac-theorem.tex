\lemma {
  Обозначим $ Lie(G) = L = L_0 \oplus L_1 $. $ Lie(G_{ev}) = L_0 $.
}
\proof {
  \qedhere
}

\lemma \label{abelian G and Lie(G)}{
  Аффинная групповая суперсхема $ G $ абелева $ \iff $ $ \Lie(G) $ абелева.
}
\proof {
  \qedhere
}

\theorem[Кац] \label{kac} {
  Супералгебра Ли $ L = L_0 \oplus L_1 $ разрешима $ \iff $ разрешима алгебра Ли $ L_0 $.
}
Доказательство можно найти в статье \cite{kac}.

\theorem{
  Пусть $ \charc K = 0 $, $ G $ - связная аффинная групповая суперсхема.
  $ G $ разрешима $ \iff $ $ \Lie(G)$ разрешима $ \iff $ $ G_{ev} $ разрешима.
}
\proof{
  1) тут ссылка на теорему Каца и на предыдущие леммы. из них следует вторая эквивалентность

  2) Предположим, что $ G $ разрешима, т.е. для некоторого $ n \in \mathbb{N} $
  $$ G \rhd G' \rhd G'' \rhd \ldots \rhd G^{(n)} = 1 $$
  % Exact sequence for first derived subgroup
  Рассмотрим $ G \rhd G' \rhd G'' $, следовательно, имеем точную последовательность
  $$ 1 \to G' \to G \to G/G' \to 1 , $$ которая эквивалентна точной последовательности для супералгебр Ли:
  $$ 0 \to \Lie(G') \to \Lie(G) \to \Lie(G/G') \to 0 $$.
  Все факторы субнормальной цепочки абелевы $ \iff $ $ G/G' $ абелев,
  откуда по лемме ~\ref{abelian G and Lie(G)} получаем, что $ \Lie(G/G') $ абелева.
  \qedhere
}
